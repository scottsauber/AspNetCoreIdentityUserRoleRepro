# AspNetCoreIdentityUserRoleRepro

Repro steps:
1. Run the app
2. Register a user
3. Go to the root Index page (should redirect on Register) where it will add 2 user roles with diff company Id's, and then it will fail.
